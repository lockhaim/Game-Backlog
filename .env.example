# Database (Supabase Postgres URL goes here later)
DATABASE_URL=postgresql://username:password@host:5432/dbname

# Auth (we'll wire NextAuth later)
NEXTAUTH_SECRET=your-random-secret
NEXTAUTH_URL=http://localhost:3000

# Steam (required for imports)
STEAM_WEB_API_KEY=your-steam-web-api-key
STEAM_USER_ID=your-steamid64   # optional; used for bulk owned-games import

# Optional providers (leave blank for now)
SUPABASE_URL=
SUPABASE_ANON_KEY=
